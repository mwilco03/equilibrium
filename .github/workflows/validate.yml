name: Validate

on:
  pull_request:
    paths:
      - "data/techniques/**"
      - "schema/**"
      - ".github/scripts/check-dc-refs.mjs"
      - ".github/workflows/validate.yml"
  push:
    branches: [main]
    paths:
      - "data/techniques/**"
      - "schema/**"
      - ".github/scripts/check-dc-refs.mjs"
      - ".github/workflows/validate.yml"

# Cancel any in-flight Validate run on the same ref when a new commit lands.
# Pure cost-control: avoids stacking redundant runs when commits arrive close
# together.
concurrency:
  group: validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  schema:
    name: JSON Schema validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Install ajv-cli
        run: npm i -g ajv-cli@5 ajv-formats@3

      - name: Validate every technique record
        run: |
          set -euo pipefail
          shopt -s nullglob
          fail=0
          for f in data/techniques/*.json; do
            echo "::group::$f"
            if ! ajv validate \
                --spec=draft2020 \
                -c ajv-formats \
                -s schema/equilibrium.schema.json \
                -d "$f" \
                --strict=false; then
              fail=1
            fi
            echo "::endgroup::"
          done
          exit "$fail"

      - name: Cross-reference data components in vendor detections
        run: node .github/scripts/check-dc-refs.mjs
